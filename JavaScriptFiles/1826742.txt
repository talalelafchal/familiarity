removing a div and focusing input field next to it
<p>my html is like this</p> <pre><code>&lt;td&gt; &lt;input type="text" class="textinput" id="file_descr_0" name="file_descr[0]" value='' /&gt; &lt;div id="mean" onClick="removeElement('mean');"&gt;{L_FileDescr}&lt;/div&gt; &lt;/td&gt; </code></pre> <p>and to remove the div i am using this function</p> <pre><code>function removeElement(div){ var d2 = document.getElementById(div); var d1 = d2.parentNode; d1.removeChild(d2); } </code></pre> <p>I wanted to know how I can make this removing element dynamic because there are lot of divs which are to be removed and because of this function i will have to give each one a id.</p> <p>and also after removing how can i focus the input field.</p> <p>Thank You.</p>
<p>you could pass the event in and use event.target</p> <pre><code>function removeElement(event) { //... } </code></pre> <p>the onclick would just be onclick="removeElement"</p>
<pre><code>&lt;div onClick="removeElement(this);"&gt;{L_FileDescr}&lt;/div&gt; function removeElement(d2){ var d1 = d2.parentNode; d1.removeChild(d2); // edit. also: d1.nextSibling.firstChild.focus(); } </code></pre>
<p>If the input is always the element before the div, you could do something like this</p> <pre><code>&lt;input type="text" /&gt; &lt;div onClick="removeElementAndFocusInput(this);"&gt;stuff&lt;/div&gt; function removeElementAndFocusInput(elem){ var toRemove = elem; toRemove.previousSibling.focus() // set focus to the input var parent = toRemove.parentNode; parent.removeChild(toRemove); // remove the div } </code></pre>