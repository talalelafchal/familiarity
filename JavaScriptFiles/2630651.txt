How to check whether the Input Text Field is visible or hidden in Javascript?
<p>I have an input text field which has </p> <pre><code>style: "visibility: visible" </code></pre> <p>or</p> <pre><code>style: "visibility: hidden" </code></pre> <p>What is the easiest way to find out if it is visible or not ?</p> <p>Suppose the input text field is E. What should be the condition here:</p> <pre><code>if &lt;something with E&gt; { alert("The text filed is visible !!"); } </code></pre> <p>?</p> <p>Thanks in advance !</p>
<p>Like this:</p> <pre><code>if(element.style.visibility == "visible") { alert("The text filed is visible !!"); } </code></pre> <p>If you also need code to get the element, here's an example by the id attribute:</p> <pre><code>var element = document.getElementById("myInputID"); if(element.style.visibility == "visible") { alert("The text filed is visible !!"); } </code></pre> <p>The above code would find an element like this (note IDs should be unique):</p> <pre><code>&lt;input type="text" id="myInputID" /&gt; </code></pre>
<pre><code>if(document.getElementByid(YOUR_OBJECT_ID_HERE).style.visbility == 'hidden') { alert('Hidden'); } </code></pre> <p>This should work.</p>