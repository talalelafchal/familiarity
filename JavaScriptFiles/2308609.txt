Change CSS Element with JS,JS?
<p>I have about 40 questions on a Question/Anwser script now I want to hide all the other messages then the one with the id clicked but the windows overlap, So how would I hide all the Other ids and show just one with Javascript?</p>
<blockquote> <p>The following examples will vary depending on your HTML structure. They are meant as examples, not solutions for your specific situation.</p> </blockquote> <p>Changing the style of a single element can be done rather quickly:</p> <pre><code>document.getElementById("foo").style.display = "none"; </code></pre> <p>You could hide all others like this:</p> <pre><code>var myDivs = document.getElementsByTagName("DIV"); for (var i = 0; i &lt; myDivs.length; i++) { if (myDivs[i].id != "foo") { myDivs[i].style.display = "none"; } else { myDivs[i].style.display = "block"; } } </code></pre> <p>If you're open to using a framework, like <a href="http://jquery.com" rel="nofollow noreferrer">jQuery</a>, you could do the same thing even quicker:</p> <pre><code>$(".question .title").click(function(){ $(this) .next().slideDown() .closest(".question").siblings() .find(".body").slideUp(); });​ </code></pre> <p>Online Demo of jQuery: <a href="http://jsbin.com/opisa/edit" rel="nofollow noreferrer">http://jsbin.com/opisa/edit</a></p>