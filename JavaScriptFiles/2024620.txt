JavaScript replace img path
<p>Hello JavaScript guru's. I have a simple JS question on how to replace all IMG src path's on a page.</p> <p>Currently, my IMG tags look like:</p> <blockquote> <pre><code>&lt;img src="path/to/image.jpg" alt="" /&gt; </code></pre> </blockquote> <p>Output desired:</p> <blockquote> <pre><code>&lt;img src="../image.jpg" alt="" /&gt; </code></pre> </blockquote> <p>So, when a page is loaded, it will loop through all the IMG tags and replace the SRC path. Thanks in advance for the assistance!</p>
<pre><code>for (var image, src, images = document.images, l=images.length, i=0; i&lt;l; i++){ image = images[i]; src = image.src; image.src = ".." + src.substring(src.lastIndexOf("/")); } </code></pre>