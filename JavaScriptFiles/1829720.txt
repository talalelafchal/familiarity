replace onmousedown with javascript
<p>Does anyone know how can I replace onmousedown value with javascript?</p> <p>for example:</p> <pre><code>&lt;input style="display:inline" type="text" autocomplete="off" name="endDateAdd" id="endDate" onkeydown="return isNumberKey(event,this)" onmousedown="initCal(this, '{$minCal}', null)" value="{$endDate}"&gt; </code></pre> <p>For the code above, I want to replace <code>onmousedown="initCal(this, '{$minCal}', null)" to onmousedown="initCal(this, '', null)"</code>. Does anyone know how to do it in javascript?</p>
<p>Assuming you're talking about changing the event handler for the element when it's already on the page:</p> <pre><code>document.getElementById('endDate').onmousedown= function() { initCal(this, '', null); }; </code></pre>