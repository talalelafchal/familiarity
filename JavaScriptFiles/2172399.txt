What's wrong with this Javascript code?
<pre><code>&lt;a href="javascript:document.getElementById('create_table').style.display='block'"&gt;Insert Table&lt;/a&gt; </code></pre> <p>The code works perfectly fine in Google Chrome but in Internet Explorer and Firefox it just redirects to a page with the text "block"</p>
<p>Your qoutes are wrong:</p> <pre><code>javascript:document.getElementById('create_table').style.display=''block </code></pre> <p>It must be:</p> <pre><code>javascript:document.getElementById('create_table').style.display='block' </code></pre> <p>But you shouldn’t use <code>javascript:</code> pseudo-protocol anyway. Better <a href="http://en.wikipedia.org/wiki/Progressive_enhancement" rel="nofollow noreferrer">use JavaScript to only enrich your document</a>.</p>
<p>You should <a href="http://crisp.tweakblogs.net/blog/313/the-useless-javascript-pseudo-protocol.html" rel="nofollow noreferrer">never</a> use the <code>javascript:</code> pseudoprotocol. Use the <code>click</code> event for this. Besides, also watch the quotes.</p> <p>Here's the correct approach:</p> <pre><code>&lt;a href="#" onclick="document.getElementById('create_table').style.display='block'; return false;"&gt;Insert Table&lt;/a&gt; </code></pre> <p>Note that I (optionally) returned false here to block the default action.</p>