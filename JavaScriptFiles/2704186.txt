convert xml.documentElement.getElementsByTagName(&quot;marker&quot;)[index].getAttribute(temp) to string in javascript
<p>I am parsing xml file in javascript and after that want to cancatenate all the the data into string. but failing to do the same and it is returning undefined.</p> <pre><code> GDownloadUrl("./include/dataemp2.xml", function(data) { var xml = GXml.parse(data); markers = xml.documentElement.getElementsByTagName("marker"); for(var t=0;t&lt;18;t++) { var temp= markers[index].getAttribute("address"); html = html + temp; } }); </code></pre> <p>it is returning as undefined because temp is not concatenating in "html"; whereas when i do this like html = html +markers[index].getAttribute("address"); it is giving me expected output;</p>
<p>your var <code>temp</code> is being re-declared inside your for loop, the <code>index</code> wasn't declared (I presume you meant <code>t</code>.</p> <pre><code> GDownloadUrl("./include/dataemp2.xml", function(data) { var xml = GXml.parse(data); markers = xml.documentElement.getElementsByTagName("marker"); var temp, html; for(var t=0;t&lt;18;t++){ temp = markers[t].getAttribute("address"); html += temp; } alert(html); }); </code></pre>