Javascript methods, classes and events
<p>how should I document this piece of code:</p> <pre><code>// Is this class? colors = { // Is this method? "red" : function() { // Do something... } // Still method? "black" : { // So what is this? "black-1" : function() { /* Do something */ } } } </code></pre> <p>I am using YUI Doc.</p> <p>These tags <strong>are available</strong></p> <ul> <li>@module</li> <li>@class</li> <li>@method</li> <li>@event</li> <li>@property</li> </ul>
<p>From using YUI, its docs, and a quick skim of the YUI Doc explanation page, it's my impression that it assumes that you're using <a href="http://ajaxian.com/archives/a-javascript-module-pattern" rel="nofollow noreferrer">the module pattern</a>.</p> <p>You don't have a module.</p> <ul> <li><code>colors</code> => @static @class</li> <li><code>red</code> => @method</li> <li><code>black</code> => @property</li> <li><code>black-1</code> => "internal" (ish) so it's not anything under this taxonomy.</li> </ul>