How can i dynamically Change onmouseover, onmouseout and onClick WITH parameters?
<p>I have an image:</p> <pre><code>&lt;img id="reqPic" src="mark.png" /&gt; </code></pre> <p>I also have declared a flag earlier on in the page:</p> <pre><code>&lt;script type="text/javascript"&gt; var isToolTipEnabled = true; &lt;/script&gt; </code></pre> <p>I now want to be able to check the flag and if its true, assign the onmouseover and onmouseout events. However, the onmousover event has to be changed to another function called Tip('string') which takes in a string. I have seen other questions on here on how to change this but it I dont see how I can pass paramters to the new function I want to change to. </p> <p>The onClick would be changed to something like this:</p> <p>onClick="javascript:toggleHelp('reftypeHelp');";</p> <p>Any help on this would be greatly appreciated.</p> <p>Thanks!</p>
<p>document.getElementById("reqPic").onmouseover = function() { Tip('This is the string.'); };</p>
<p>You could have an onload event on your body tag which calls a function that checks necessary values.</p> <pre><code>&lt;script type="text/javascript"&gt; var isToolTipEnabled = true; function eventAdder() { if (isToolTipEnabled) { var img = document.getElementById('reqPic'); img.onmouseover = function() { Tip('string'); } img.onmouseout = whateverElse; } } &lt;/script&gt; &lt;body onload="eventAdder();"&gt; </code></pre>
<p>with jQuery you could use the hover function but yeah creating an inline function works too</p>