What Javascript function is used to check for a certain browser plugin?
<p>What Javascript function is used to check for a certain plugin? Such as for Silverlight, Flash, Quake Live, Real Player...</p>
<p>"Use the mimeTypes property to see if the browser supports a particular MIME Type, such as application/x-shockwave-flash. If it does, you know that Flash Player is installed. "</p> <p><a href="http://www.adobe.com/support/flash/how/shock/javaplugs/javaplugs02.html" rel="nofollow noreferrer">http://www.adobe.com/support/flash/how/shock/javaplugs/javaplugs02.html</a></p>
<p>There are various ways:</p> <pre><code>if (navigator.plugins) { for (var i = 0; i &lt; navigator.plugins.length; i++) { //blah blah blah } } else if (navigator.mimeTypes) { var useFlash = navigator.mimeTypes &amp;&amp; navigator.mimeTypes["application/x-shockwave-flash"] &amp;&amp; navigator.mimeTypes["application/x-shockwave-flash"].enabledPlugin; } else { //Usually IE crap....lol } </code></pre> <p>Basically in summary:</p> <ul> <li><code>navigator.plugins</code></li> <li><code>navigator.mimeTypes</code></li> </ul> <p>and For IE, check this site as example: <a href="http://www.rgagnon.com/jsdetails/js-0056.html" rel="nofollow noreferrer">http://www.rgagnon.com/jsdetails/js-0056.html</a></p> <p>Apparently you can use VBScript to check plugins (only in IE).</p>