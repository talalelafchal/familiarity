how can put a javascript variable in img tag
<p>I need to put a javascript variable as image source. That is my image tag <strong>should be in this format</strong> here i am adding my script</p> <pre><code>document.getElementById("pricefilter").innerHTML ='&lt;img src="variablename/spinner.gif" title="Loading..." alt="Loading..."&gt;'; </code></pre> <p>The <strong>variablename</strong> carries my image path. How can I put this in double quates.</p> <p>Please help to solve this</p>
<p>If i get your question correctly use </p> <p><code>document.getElementById("pricefilter").src=ur variable;</code></p> <p>One of previuos <a href="http://stackoverflow.com/questions/116967/img-src-tags-and-javascript">stackoverflow ques</a> similar to urs showed this</p> <pre><code>&lt;img src="blank.png" id="image" alt="just nothing"&gt; &lt;script type="text/javascript"&gt; document.getElementById('image').src = "yourpicture.png"; &lt;/script&gt; </code></pre>
<p>Strings can be added together, if <code>variablename</code> contains the path:</p> <pre><code>document.getElementById("pricefilter").innerHTML ='&lt;img src="' + variablename + '/spinner.gif" title="Loading..." alt="Loading..."&gt;'; </code></pre>