Set boundary ODE solver
<p>I'm solving differential equations using an <code>ODE15s</code> solver. However with specific parameters the solution becomes unreal. When you have a reaction like: </p> <p><code>A + B &lt;--&gt; C &lt;--&gt; D + E and E + F --&gt; G</code></p> <p>The concentration of <code>A</code> and <code>E</code> becomes less than zero if I start with an high initial concentration of <code>F</code>. This is impossible in practice.</p> <p>Is there a way that I can specify boundary conditions in the ODE solver? For instance:</p> <p><code>If A&lt;0 : A=0</code></p>