select the lower number of a row (matlab)
<p>With any given matrix, how do you select the lower number in each row? I have something like this so far:</p> <pre><code>matrix = magic (5); rc = size (mat); r = rc (1); for i = 1:r disp (matrix (i,:)) </code></pre>
<p>To get the minimum number greater than 0 within each row (according to comments): replace non-positive numbers by <code>NaN</code> and then minimize along each row:</p> <pre><code>matrix = randn(5,6); %// example data matrix2 = matrix; %// copy of data matrix2(matrix2&lt;=0) = NaN; %// replace non-positive values by NaN m = min(matrix2,[],2); %// minimize along each row </code></pre>