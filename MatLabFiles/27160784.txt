Understanding 1D FFT on 3D data using MATLAB
<p>Could someone kindly explain what the following <code>MATLAB</code> line do?</p> <pre><code>fft( data3D, N, dim ); </code></pre> <p><code>data3D</code> is an array of size <code>(NY, NX, NZ)</code>. <code>MATLAB</code> documentation says</p> <blockquote> <p><code>fft(X,[],DIM)</code> or <code>fft(X,N,DIM)</code> applies the <code>fft</code> operation across the dimension <code>DIM</code>.</p> </blockquote> <p>What exactly <code>fft</code> calculates across a dimension when the input is a <code>3D</code> array?</p>
<p>To expand on @Oliver Charlesworth, the command <code>fft(X,[],DIM)</code> should do this:</p> <pre><code>fft_values=zeros(size(X)); %pre-allocate the output array if (DIM==1) for I=1:size(X,2) for J=1:size(X,3) fft_values(:,I,J) = fft(squeeze(X(:,I,J)); end end elseif (DIM==2) for I=1:size(X,1) for J=1:size(X,3) fft_values(I,:,J) = fft(squeeze(X(I,:,J)); end end elseif (DIM==3) for I=1:size(X,1) for J=1:size(X,2) fft_values(I,J,:) = fft(squeeze(X(I,J,:)); end end end </code></pre>