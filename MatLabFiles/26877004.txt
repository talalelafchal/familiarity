Arrange two output pairs into a single matrix on MATLAB
<p>I'm using a command on MATLAB that gives me the output in the following format:</p> <pre><code>ans = 120.5000 226.5000 ans = 192.5000 223.0000 </code></pre> <p>I get this two outputs pairs at the same time doing:</p> <pre><code>s = regionprops(binFrameRed, 'centroid', 'area'); s.Centroid; </code></pre> <p>Where s.Centroid are the centroid coordinates of two red squares on an image.</p> <p>I would like to arrange these outputs into a single matrix, where the first row is given by the first output pair and the second row is given by the second output pair.</p> <p>I know that I can do this using <code>reshape</code>, but for me it would be better if I can use a more direct way.</p> <p>Does anybody know how can I implement this?</p>
<p>This should do the trick:</p> <pre><code>centroids = cat(1, s.Centroid); </code></pre> <p>Example output for some binary image with two centroids:</p> <pre><code>s.Centroid ans = 316.0324 202.4018 ans = 506.7500 135.5469 centroids = cat(1, s.Centroid); centroids centroids = 316.0324 202.4018 506.7500 135.5469 </code></pre>
<p>Just for the fun of using square brackets:</p> <pre><code>[s(:,1).Centroid;s(:,2).Centroid] </code></pre>