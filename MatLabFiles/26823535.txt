How to use cell in matlab for this particular code
<p>Here is part of my code:</p> <pre><code>for i=1:12 APleft{i}=[-1 0 pointsleftnormalizedplane(1,i) 0; 0 -1 pointsleftnormalizedplane(2,i) 0; -RE_1(1,1)+pointsrightnormalizedplane(1,i)*RE_1(3,1) -RE_1(1,2)+pointsrightnormalizedplane(1,i)*RE_1(3,2) -RE_1(1,3)+pointsrightnormalizedplane(1,i)*RE_1(3,3) -T_tran1(1)+pointsrightnormalizedplane(1,i)*T_tran1(3); -RE_1(2,1)+pointsrightnormalizedplane(2,i)*RE_1(3,1) -RE_1(2,2)+pointsrightnormalizedplane(2,i)*RE_1(3,2) -RE_1(2,3)+pointsrightnormalizedplane(2,i)*RE_1(3,3) -T_tran1(2)+pointsrightnormalizedplane(2,i)*T_tran1(3)]; [U{i},S{i},V{i}]=svd(APleft{i}); end </code></pre> <p>Why is always gives error? The problem is in the last line before <code>end</code>. I used the cell function in matlab and actually I just want to do the Singular value decomposition for a loop of different matrices but how can I get the decomposition results for each matrix?</p> <p>Thanks in advance!</p> <p>The error MATLAB showed is:</p> <blockquote> <p>Comma separated list expansion has cell syntax for an array that is not a cell.</p> </blockquote> <pre><code>Error in reconstruction (line 79) [U{i},S{i},V{i}]=svd(APleft{i}); </code></pre>