MATLAB: How to concatenate all struct elements into a string?
<p>"metadata" is a struct consists of many different elements such as char, double and struct. <img src="https://i.stack.imgur.com/rWXTf.png" alt="enter image description here"></p> <p>"cancatenate1" is a string resulted from the concatenation of every cell in the metadata.</p> <pre><code>char epr; epr = cellfun(@num2str, struct2cell(metadata),'UniformOutput', false); </code></pre> <p>Console shows below...</p> <pre><code>"Undefined function 'abs' for input arguments of type 'struct'. Error in num2str (line 65) xmax = double(max(abs(widthCopy(:))));" </code></pre>