MATLAB MIRtoolbox - mirtempo function
<p>I just want to know how functions like <code>mirtempo()</code>, which returns a <code>mirscalar</code> object to a direct format that MATLAB can understand.</p> <p>I have a script which contains</p> <pre><code>tempo = mirtempo('file'); </code></pre> <p>this makes <code>tempo</code> a <code>mirscalar</code> object.</p> <p>How to convert <code>tempo</code> variable to a double value so it could be used in a script?</p> <p>I need to store the tempo value of multiple songs for song classifications. I am not able to do that with <code>mirtempo()</code>. Rather I can display the tempo value in command window like:</p> <pre><code>tempo </code></pre> <p>Output would be like: </p> <pre><code>"The Tempo related to file classical.00001.au is 165.5543 bpm" </code></pre> <p>Could somebody please help me out? Thanks in advance</p>
<p>here is the trick:</p> <pre><code>tempo = mirtempo('file'); test=get(tempo,'Data'); test{1,1}{1,1} ans = [120.0106] </code></pre>
<pre><code>mirgetdata(tempo) ans = 120.0106 </code></pre>