variable input changing in time, ode45 MATLAB
<p>I want to compute the solution of a first order differentiable equation using ode45 in MATLAB.</p> <p>But I want to compute the solution in precise times and knowing a input variable in those instants.</p> <p>Let t be the vector with n time instants where I want to compute the solution, and u the input vector also with n values, where $u(k)$ is the value of $u$ at instant $k$, and $k$ belong to $t$.</p> <p>I call the ode45 function as:</p> <pre><code>[t,T_dot]=ode45(@f_mass,t,.01,odeset,u); </code></pre> <p>and f_mass is:</p> <pre><code>function T_dot=f_mass(t,T,u) T_dot=(1-u)*T; end </code></pre> <p>How can I specify that u is u(t)?</p> <p>Thanks</p>