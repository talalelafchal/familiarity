How do I take the red out of an image using Matlab?
<p>I've just used this code to keep just the red in an image:</p> <pre><code>hsvImage = rgb2hsv(imread(filename)); hPlane = 360.*hsvImage(:,:,1); sPlane = hsvImage(:,:,2); nonRedIndex = (hPlane &gt; 25) &amp; (hPlane &lt; 340); sPlane(nonRedIndex) = 0; hsvImage(:,:,2) = sPlane; rgbImage = hsv2rgb(hsvImage); </code></pre> <p>Here's a set of my images with only red:</p> <p><img src="https://i.stack.imgur.com/1FMC6.jpg" alt="Only red"></p> <p>Now I'm trying to change it so it takes out the red, like so:</p> <pre><code>hsvImage = rgb2hsv(imread(filename)); hPlane = 360.*hsvImage(:,:,1); sPlane = hsvImage(:,:,2); nonRedIndex = (hPlane &lt; 25) &amp; (hPlane &gt; 340); sPlane(nonRedIndex) = 0; hsvImage(:,:,2) = sPlane; rgbImage = hsv2rgb(hsvImage); </code></pre> <p>But for some reason the red stays in the image. Any thoughts on why this is happening?</p> <p>Here's the images that should have none of the red in the first image:</p> <p><img src="https://i.stack.imgur.com/MYezi.jpg" alt="No Red"></p> <p>It should just the opposite from the other image, right? I'm not sure why it isn't getting rid of the red.</p>