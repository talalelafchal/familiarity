didSelectRowAtIndexPath not working after search
Am using the code below to search through a tableView  func searchBar(searchBar: UISearchBar, textDidChange searchText: String) { filteredProducts = food.filter({ (text) -> Bool in let tmp: NSString = text.store_name let range = tmp.rangeOfString(searchText, options: NSStringCompareOptions.CaseInsensitiveSearch) return range.location != NSNotFound }) if(filteredProducts.count == 0){ inSearchMode = false; } else { inSearchMode = true; } self.tableView.reloadData() print("searchText \(searchText)") } And my cellForRowAtIndexPath looks like this  var foo: Food! if inSearchMode{ foo = filteredProducts[indexPath.row] }else{ foo = food[indexPath.row] } An here is my didSelectRowAtIndexPath  var foo:Food! if inSearchMode{ foo = filteredProducts[indexPath.row] }else{ foo = food[indexPath.row] } But when i select any row after searching the data from another row is displayed. It is not showing the row data i passes to another view it is showing another view own