UISegmentedControl parsing data into Title
I have 2 ViewControllers. I am trying to pass data from a textfield (what the user will type) to the title of the Segmented control. Practically changing the tile to the user input. It is always crashing at this line. I can not figure out how to make it work: let title = DestViewController.segment.titleForSegment(at: 0) ViewController (that holds the text field ) import UIKit class ViewController: UIViewController { @IBOutlet var textField: UITextField! override func viewDidLoad() { super.viewDidLoad() // Do any additional setup after loading the view, typically from a nib. } override func didReceiveMemoryWarning() { super.didReceiveMemoryWarning() // Dispose of any resources that can be recreated. } override func prepare(for segue: UIStoryboardSegue, sender: Any?) { let DestViewController: ViewTwo = segue.destination as! ViewTwo //DestViewController.myLabelText = textField.text! let title = DestViewController.segment.titleForSegment(at: 0) if title == textField.text{ DestViewController.segment.setTitle("New Title", forSegmentAt: 0) }else{ print("Error!") } } } ViewTwo (where the segmented control is) import UIKit class ViewTwo: UIViewController { @IBOutlet var myLabel: UILabel! @IBOutlet var segment: UISegmentedControl! var myLabelText = String() @IBAction func segmentAction(_ sender: AnyObject) { return } override func viewDidLoad() { super.viewDidLoad() myLabel.text = myLabelText // Do any additional setup after loading the view. } override func didReceiveMemoryWarning() { super.didReceiveMemoryWarning() // Dispose of any resources that can be recreated. } /* // MARK: - Navigation // In a storyboard-based application, you will often want to do a little preparation before navigation override func prepare(for segue: UIStoryboardSegue, sender: Any?) { // Get the new view controller using segue.destinationViewController. // Pass the selected object to the new view controller. } */ }