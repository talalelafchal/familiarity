How to compare genericType:Integer with specific integer type?
My goal is to iterate the elements in a container, and skip any element that is beyond the capacity. My code and the warning I get: class IntegerFiddler <Container, Element> where Container: IteratorProtocol, Container.Element == Element, Element: Integer { var container: Container let capacity = UInt.max init(container: Container) { self.container = container } func fiddle() { while let integer: Element = self.container.next() { // Skip if the integer is beyond capacity // guard integer < Element(self.capacity) else { continue } } } } How to make this work? Thanks