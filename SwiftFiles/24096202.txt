UIViewController initialization in Swift throws compile error
<p>I'm trying to instantiate a UIViewController with the designated initializer and a xib file View.xib like so</p> <pre><code>var appBundle = NSBundle.mainBundle() var controller = UIViewController.init(nibName: "View", bundle: appBundle) </code></pre> <p>But I get the compile error:</p> <pre><code>Command /Applications/Xcode6-Beta.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/bin/swift failed with exit code 254 </code></pre> <p>Is anyone else getting this error? I don't know what I'm doing wrong.</p>
<p>try this instead: </p> <pre><code>var appBundle = NSBundle.mainBundle() var controller = UIViewController(nibName: "ViewController", bundle: appBundle) </code></pre>