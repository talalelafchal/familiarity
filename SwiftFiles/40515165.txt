Page View Within Tableview cell producing multiple layers
I am adding a page view controller to my table view cell. It all works as expected except one thing. When attempting to scroll before index 0 and after the last index, I see an image behind the page view controller. I looked at the ui heirarchy and saw this: My code for adding the page view to my table view is: override func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell { let typeOfStoryToDraw = contents[(indexPath as NSIndexPath).row].type let content = self.contents[(indexPath as NSIndexPath).row] switch typeOfStoryToDraw { case "photo-galleries"?: let cell = tableView.dequeueReusableCell(withIdentifier: "pageViewHomeCell") cell?.backgroundColor = UIColor.black let customPageVC = CustomPageVC() customPageVC.contents = self.contents self.addChildViewController(customPageVC) customPageVC.view.frame = cell!.contentView.bounds customPageVC.didMove(toParentViewController: self) customPageVC.isFullScreen = false cell!.contentView.addSubview(customPageVC.view) let tap = UITapGestureRecognizer(target: self, action: #selector(HomeTVC.handleTapForPhotoVCTransition)) tap.delegate = self cell!.contentView.addGestureRecognizer(tap) return cell! }} I am implementing another page view controller in my app successfully and the code for both are practically identical. Any Ideas about how this is happening?