Newest Real Time post shows nil values from Firebase
I am using the child added function of Firebase to show new user posts as they are added. For all the posts added before the user views the controller, the collection view properly loads the data. Anything added in real time after the user is in the view returns nil for all the values in the struct.  let query = ref.child("AllPosts").child(selectedPlace).queryLimitedToLast(10) handle = query.observeEventType(.ChildAdded, withBlock: { (snap) in if snap.exists() { let postInfo = PostStruct(snap: snap) //when breakpointing this line below it returns nil values for the newly added post self.regionPosts.append(postInfo) self.regionPosts.sortInPlace({$0.timeDifference < $1.timeDifference}) dispatch_async(dispatch_get_main_queue(), { self.collectionView?.reloadData() }) } This is the code for the struct that I am passing the snapshot to properly assign the values to my model  struct PostStruct { var displayName: String? var userComment: String? var timeDifference: NSTimeInterval? var timeElapsed: String? var locationName: String? var colorTag: Int? var color: UIColor? init(snap: FIRDataSnapshot) { let snapshot = snap.value as! NSDictionary userComment = snapshot["userComment"] as? String displayName = snapshot["username"] as? String locationName = snapshot["locationName"] as? String colorTag = snapshot["tag"] as? Int if let setTag = colorTag { color = Helper.addColorToTag(setTag) } let timeStamp = snapshot["checkTime"] as? NSTimeInterval if let setTimeStamp = timeStamp { let setDate = NSDate(timeIntervalSince1970: setTimeStamp/1000) let interval = fabs(setDate.timeIntervalSinceNow) timeDifference = interval var diff = Helper.findTimeElapsed(setTimeStamp) if diff == "0m" { diff = "Now" } timeElapsed = "\(diff)" } } } What would be different about the newest retrieved real time post with a child added function? Thanks in advance as I am not sure what is causing the problem