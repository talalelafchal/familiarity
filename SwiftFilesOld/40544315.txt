How can use pinch function on textfield
I want my textfield both draggable and pinching. It is draggable into that imageview but I also want that pinch-able. I added the code, it doesn't give any failure but it is not working either. I also try codes in this post but can't figure out to make them work. iOS: Scaling UITextView with pinching? EDIT: I changed the pinched view function but still no chance. let pinchRecognizer = UIPinchGestureRecognizer() override func viewDidLoad() { super.viewDidLoad() //Font Default bottomTextField.font = UIFont(name: (bottomTextField.font?.fontName)!, size: 30) // Drag let gesture = UIPanGestureRecognizer(target: self, action: #selector(ViewController.draggedView(_:))) bottomTextField.addGestureRecognizer(gesture) bottomTextField.isUserInteractionEnabled = true //Pinch pinchRecognizer.addTarget(self, action: #selector(ViewController.pinchedView(_:))) bottomTextField.addGestureRecognizer(pinchRecognizer) } func userDragged(gesture: UIPanGestureRecognizer){ let loc = gesture.location(in: MyimageView) self.bottomTextField.center = loc } func pinchedView(_ sender: UIPinchGestureRecognizer) { self.view.bringSubview(toFront: sender.view!) sender.view!.transform = sender.view!.transform.scaledBy(x: sender.scale, y: sender.scale) bottomTextField.font = UIFont(name: (bottomTextField.font?.fontName)!, size: (bottomTextField.font?.pointSize)! * sender.scale) sender.scale = 1.0 } func draggedView(_ sender: UIPanGestureRecognizer) { guard let senderView = sender.view else { return } var translation = sender.translation(in: view) translation.x = max(translation.x, MyimageView.frame.minX - bottomTextField.frame.minX) translation.x = min(translation.x, MyimageView.frame.maxX - bottomTextField.frame.maxX) translation.y = max(translation.y, MyimageView.frame.minY - bottomTextField.frame.minY) translation.y = min(translation.y, MyimageView.frame.maxY - bottomTextField.frame.maxY) senderView.center = CGPoint(x: senderView.center.x + translation.x, y: senderView.center.y + translation.y) sender.setTranslation(.zero, in: view) view.bringSubview(toFront: senderView) }