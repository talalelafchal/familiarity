Swift: editActionsForRowAtIndexPath can't access indexPathForSelectedRow?
When accessing tableView(tableView,editActionsForRowAtIndexPath) I want to call a function on the previously selected UITableViewCell, but tableView.indexPathForSelectedRow is returning nil. Here is the code I am using. The block inside the if statement does not run and tableView.indexPathForSelectedRow returns a nil value.  func tableView(tableView: UITableView, editActionsForRowAtIndexPath indexPath: NSIndexPath) -> [UITableViewRowAction]? { if let selectedRow = activityTableView.indexPathForSelectedRow { updateCell(atIndexPath: selectedRow) } var rowActions = [UITableViewRowAction]() .... return rowActions }