Parse Server OAuth Google SingIn save users in Parse -Swift
I'm using Google SingIn for my app. And my problem is when i try to save the information of the user in parse server i cant because parse say that the user is all ready sign in and i dont no how to do. This is for iOS and i programing in Swift but y you have the answer in Objective C is helpfull aswell. Thanks! public func sign(_ signIn: GIDSignIn!, didSignInFor user: GIDGoogleUser!, withError error: Error!) { if let err = error { print(err) } else { let fullName = user.profile.name let givenName = user.profile.givenName let familyName = user.profile.familyName let email = user.profile.email let userParse = PFUser() userParse.email = email userParse.username = fullName userParse.signUpInBackground { succeded, error in if (succeded){ print("User save in Parse") }else if let error = error { print(error) } } print("\(fullName!), \(givenName!), \(familyName!), \(email!)") self.present(PersonalInfoVC(), animated: true, completion: nil) } } This is de code I'm using