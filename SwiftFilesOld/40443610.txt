swift + global array is null after the task
This is the code: var loans = [Loan]() func getLastestLoans() { let task = URLSession.shared.dataTask(with: URLRequest(url: loanUrl), completionHandler: { (data, response, error) -> Void in if let error = error { print(error) return } // Parse JSON data if let data = data { self.loans = self.parseJSONData(data: data) print(self.loans.count) //Now the value of loans is not null OperationQueue.main.addOperation({ self.tableView.reloadData() }) } }) task.resume() } But after the task ,the loans is null. loans is the global variables, why there is no value in loans after i run the function getLastestLoans.