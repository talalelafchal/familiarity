Save object in CoreData
I want to save object in CoreData, but I don't want one object duplicate add in data base, there is my code for add entity in CoreData. in my case cart name can be same , but cart number only can added once. func createObjectCart() { let name = self.cartNameTextField.text let number = self.cartNumberTextField.text let fetchRequest: NSFetchRequest<Cart> = Cart.fetchRequest() fetchRequest.predicate = NSPredicate(format: "number == %@", number!) var fetchCart : [Cart]! stack.mainQueueContext.performAndWait(){ fetchCart = try! self.stack.mainQueueContext.fetch(fetchRequest) } if (fetchCart?.count)! > 0 { self.cart = fetchCart.first! allCarts.append(self.cart!) // allCarts.removeLast() } let newCart = NSEntityDescription.insertNewObject(forEntityName: "Cart", into: self.stack.mainQueueContext) as! Cart newCart.name = name! newCart.number = number! do { try stack.mainQueueContext.save() } catch { print("errorr") } }