Crashlytics show wrong Crash
I have an App when i install this app via XCODE it works perfectly but when i install it via Crashlytics it carshes the app and show the line number where i have this code.. This is my LoginViewController code and i am writing this code in the success block of WebServiceCall. As i see this code doesn't have any chance of crash then why Crashlytics show error and crash the entire app at this point? let userDefault = NSUserDefaults.standardUserDefaults() userDefault.setValue(self.emailTextField.text, forKey: Constants.kVaboEmail) userDefault.synchronize() userDefault.setValue(self.passwordTextField.text, forKey: Constants.kVaboPassword) userDefault.synchronize()