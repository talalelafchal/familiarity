pprof - &quot;Need a 64-bit perl to process this 64-bit profile.&quot;
<p>I've installed <a href="https://code.google.com/p/gperftools/" rel="nofollow">gperftools</a>, and got it to build and run.</p> <p>I've started to profile the CPU usage as follows:</p> <pre><code>$ g++ -o profiling file.cpp -lprofiler -ltcmalloc $ CPUPROFILE=/tmp/prof.out ./profiling PROFILE: interrupts/evictions/bytes = 244/32/4800 </code></pre> <p>But then I get the following:</p> <pre><code>$ pprof ./runmed /tmp/prof.out Using local file ./profiling. Using local file /tmp/prof.out. Need a 64-bit perl to process this 64-bit profile. </code></pre> <p>I tried reinstalling Perl, but to no avail. I'm using Mavericks 10.9.3.</p> <p>Can someone shed light on how to fix this?</p>