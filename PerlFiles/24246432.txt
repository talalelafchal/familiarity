Issue while retrieving Form id using HTML::FORM and LWP in perl
<p>I am new to perl and any help will be appreciated. There is one URL and on opening this URL,we have a form page..I want tpo log into this form using HTML::FORM.</p> <p>Here is my code: </p> <pre><code>$request-&gt;url($url); $response = $ua-&gt;get($url); $isLoginRequired = 1; my $responseCode = $response-&gt;code; if ($response-&gt;is_success) { my @forms = HTML::Form-&gt;parse($response-&gt;content,"UTF-8"); my $arrSize = @forms; @forms = grep $_-&gt;attr("id") eq "loginData", @forms; if ( !@forms ) { my $errormsg = "ERROR: No form named 'loginData' found for url $url\n"; print "$errormsg"; push( @FAILURES, $errormsg ); next; } </code></pre> <p>If I print response->content, I can see form id="loginData" </p> <p>but I am getting "ERROR: No form named 'loginData' found for url " error</p> <p>I printed @forms contents,its HTML::Form=HASH(0x896ca5c) </p> <p>If I use my @forms = HTML::Form->parse($response->decode_content,"UTF-8"); , @forms comes out to be empty.</p> <p>Kindly help..I have done lot of google but nothing helped.</p>