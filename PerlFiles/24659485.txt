Dancer Quickstart Instructions Not Working
<p>I am trying to follow the quick start instructions for Dancer, <a href="http://perldancer.org/quickstart" rel="nofollow">http://perldancer.org/quickstart</a>.</p> <p>The first command ...</p> <pre><code>curl -L http://cpanmin.us | perl - --sudo Dancer </code></pre> <p>... worked fine as far as I can tell, but the second command fails like so:</p> <pre><code>$ dancer -a MyWeb::App bash: dancer: command not found </code></pre> <p>So I figured I look to see where <code>dancer</code> got installed and I find this:</p> <pre><code>$ sudo find / -name dancer /home/red/.cpanm/work/1404922778.2704/Dancer-1.3124/script/dancer </code></pre> <p>I suspect that in install failed somewhere. Can anyone out there help me out?</p> <p>Thanks!</p>
<p>It turns out that I needed to install some rpm's:</p> <pre><code>sudo yum install perl-CPAN sudo yum install perl-LWP sudo yum install perl-libwww-perl sudo yum install perl-Time-HiRes </code></pre> <p>then re-run the <code>curl</code> command.</p>