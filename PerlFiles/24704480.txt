Changing image/css if scalar variable &gt; 0, Perl
<p>I have a header with a notification system implemented with an integer next to a <code>mail1.png</code> like so:</p> <pre><code>&lt;div class = "header"&gt; &lt;a class="notifications-dropdown-trigger"&gt;&lt;b id="num-posts"&gt;&lt;%= (scalar @post_regular) %&gt;&lt;/b&gt; &lt;img src="/images/icons/mail1.png" /&gt;&lt;/a&gt; &lt;/div&gt; </code></pre> <p>the <code>scalar @post_regular</code> retrieves and shows the amount of notifications in the database for that user as an integer.</p> <p>I want to implement something that will change the image next to it, as well as the background color of the number, whenever the <code>scalar @post_regular</code> is greater than 0. So when the user has at least one notification, I want the image next to it to display <code>mail2.png</code> instead of <code>mail1.png</code> and the background color to be <code>red</code> instead of the default.</p> <p>I'm assuming this approach should be tackled with javascript? I'm not too familiar with it and not sure how to approach it.</p>
<p>Just add logic to your img src:</p> <pre><code>&lt;img src="/images/icons/mail&lt;%= @post_regular ? 2 : 1 %&gt;.png" /&gt; </code></pre> <p>Similar functionality can be used to adjust the styling, but I'd probably recommend injecting a secondary class to set the background color instead of hardcoding the style.</p>