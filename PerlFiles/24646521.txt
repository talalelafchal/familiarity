How to perform &quot;sudo su - root&quot; without using perl modules in perl script
<p>I am logged on my server using "x" user. On the CLI i can use the command "sudo su - root" and execute subsiquent commands using root user on my server. But i am not able to run this sudo su - root command in a perl script. If possible, i need the solution without using any perl module.</p>
<p>if you wan to execute some unix commands under root environment using your own password(user x's password) then Can you try something like this?Unix command inside <strong>tilt</strong>(`)</p> <pre><code>`sudo su - root` </code></pre> <p>example:</p> <pre><code> use warnings; use strict; print "Before :"; print `whoami`; print `sudo su - root &lt;&lt;EOF whoami exit EOF`; </code></pre> <p>above script will work you with in <strong>root environment</strong>.Let me know if any clarification or issues!.</p>