Perl: grep a value in array in array
<p>How do I grep values from an array of arrays? My attempt:</p> <pre><code>my $match =grep (/value/, @array); if ( $match &lt;= 0 ) { ... } </code></pre> <p>I am always getting zero as a output which is incorrect.</p> <p>I am able to print $value after 2 <code>for</code>, loops so I think grep will only work when there is 1 loop.</p> <pre><code>for $value (array) { for my $value1 (@$value) { print $value1 }; </code></pre> <p>Thanks.</p>
<p>To grep first level of arrays into <code>@result</code></p> <pre><code>my @result = grep { grep { /search/ } @$_ } @array; </code></pre> <p>grepping final strings into <code>@result</code>,</p> <pre><code>my @result = grep { /search/ } map { @$_ } @array; </code></pre>