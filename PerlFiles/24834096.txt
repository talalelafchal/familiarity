Submitting Form to CGI with Link
<p>I currently have an HTML page with a tree, displayed roughly as such:</p> <pre><code>Family A 1 2 B 3 4 </code></pre> <p>Each of the numbers (1,2,3,4) are linked, using the following snippet of code embedded within a Perl CGI script:</p> <pre><code>&lt;li id=$level2_id&gt; &lt;input type=\"hidden\" name=\"query\"/&gt; &lt;input type=\"hidden\" name=\"dataset\"/&gt; &lt;a href=\"javascript:getgpcr('$level2_name')\"&gt;$level2_name&lt;/a&gt; &lt;/li&gt; </code></pre> <p>This, in turn, is embedded in the $tree variable in the following html:</p> <pre><code>&lt;div class="g_content"&gt; &lt;p class="g_head"&gt;GPCR Families&lt;/p&gt; &lt;form accept-charset="utf-8" name="families" action="query.cgi" method="post"&gt; &lt;ul class="mktree" id ="tree1"&gt; $tree &lt;/ul&gt; &lt;/form&gt; &lt;/p&gt; &lt;/div&gt; </code></pre> <p>The JavaScript function is as follows:</p> <pre><code>&lt;script language="JavaScript" type="text/javascript"&gt; function getgpcr ( gpcr ) { document.families.dataset.value = "Search in targets"; document.families.query.value = gpcr; document.families.submit(); } &lt;/script&gt; </code></pre> <p>The goal that I would like to achieve is to pass the values from 'query' and 'dataset' into query.cgi, but unfortunately, it does not seem to be working. I suspect that the values are not being passed properly to the CGI script, but that's just me. Where is the problem lying? I apologize for my ignorance, as I am new to CGI programming. Any help would be greatly appreciated. Thanks.</p>