Get value of OnClick
<p>This is the Perl code that makes a list of clickable group names</p> <pre><code> print "&lt;TABLE border=22&gt;"; foreach $tmp (@splitarray) { print "&lt;TR&gt;"; $links = "&lt;a href = '#' name='group' onclick='GetGroupName(this);' &gt;$tmp&lt;/a&gt;&lt;BR&gt;"; print "&lt;TD&gt;$links&lt;/TD&gt;"; } print "&lt;/TR&gt;"; print "&lt;/TABLE&gt;"; } </code></pre> <p>I would like the <code>GetGroupName</code> Perl function to return the value of the link clicked and print it</p> <p>For example</p> <pre><code>sub GetGroupName { $groupName = name of link that is clicked } print $groupName; # Which would be like group 1 </code></pre> <p>Is there a way to do this without using JavaScript? (If need be I can change how the <code>$links</code> variable is worded.)</p>