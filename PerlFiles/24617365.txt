Perl Rest::Client timing out despite setting
<p>I'm having an issue with the <a href="https://metacpan.org/pod/REST%3a%3aClient" rel="nofollow"><code>Rest::Client</code></a> module in perl. My connection is timing out after 5 minutes despite setting the timeout value to 10 minutes. I've even tried increasing the hardcoded default value in the module itself to no avail:</p> <pre><code>$client = REST::Client-&gt;new(timeout =&gt; 1200); $client-&gt;setHost('https://www.example.com'); $client-&gt;addHeader('Content-Type', 'text/x-json'); $client-&gt;setTimeout(1200); </code></pre> <p>And the code in the module that I changed, this begins on line 336:</p> <pre><code>my $ua = $self-&gt;getUseragent(); if(defined $self-&gt;getTimeout()){ $ua-&gt;timeout($self-&gt;getTimeout); }else{ #$ua-&gt;timeout(600); $ua-&gt;timeout(1200); } my $req = HTTP::Request-&gt;new( $method =&gt; $url ); </code></pre> <p>Despite these changes my requests are still timing out after 5 minutes. I'm receiving a 500 response. Is there any other place where the 5 minute value might be hardcoded in a dependent module?</p>