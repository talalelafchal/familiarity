how to send data to spawned process in expect module , so it is invisible on terminal
<p>I have simple program which asks for password , when i spawn this process and expecting 'password' , how to send password to process so it is invisible in terminal.</p> <p>password program is </p> <pre><code>(login.c) #include&lt;stdio.h&gt; int main() { int a; printf("enter password\n"); scanf("%d",&amp;a); return 0; } # gcc login.c </code></pre> <p>perl program </p> <pre><code>ask_passwd.pl #! /usr/bin/perl -w use strict; use Expect; my $passwd=Expect-&gt;spawn("./a.out"); $passwd-&gt; expect(10,'-re',"password" =&gt;sub{ $passwd-&gt; send("12345\n");); # perl ask_passwd.pl </code></pre> <p>output of program is </p> <pre><code>enter password 12345 </code></pre> <p>how to send password so it is not dispalyed.....?</p>