Executing a Perl script on Java button ActionEvent
<p>I'm trying to run a Perl script when I push a button on a JFrame. I read around and realize that I need to use Runtime.exec() to run a command, and this works in the following code:</p> <pre><code>import java.io.IOException; public class Charts { public static void main(String args[]) { try { Runtime.getRuntime().exec("perl C:/Users/ryanm/Documents/updateCharts.pl"); } catch (IOException e) { System.out.println (e.toString()); } } } </code></pre> <p>However, if I place this code in the ActionEvent for a button on a jframe it doesn't work:</p> <pre><code>import java.io.IOException; ... ... private void jButton1ActionPerformed(java.awt.event.ActionEvent evt) { try { Runtime.getRuntime().exec("perl C:/Users/ryanm/Documents/updateCharts.pl"); System.out.println ("Execute"); } catch (IOException e) { System.out.println (e.toString()); } } ... ... </code></pre> <p>The print statement in the try block prints out when I click the button, so I'm not sure what's wrong here.</p> <p>Thanks for any help!</p>