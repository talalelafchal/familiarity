Graphviz : Color multiple Node using Perl
<p>This is regarding coloring of multiple nodes in Perl (inside loop)</p> <p>Is there any way to color nodes based on some condition</p> <p><strong>Input.txt</strong></p> <pre><code>C;A D;C B;D E;D F;D </code></pre> <p><strong>Code Snippet</strong></p> <pre><code>$g = GraphViz-&gt;new( node =&gt; { shape =&gt; 'box' } ); @inp = `cat Input.txt`; foreach my $i ( @Inp ) { chomp( $i ); my ( $j1, $j2 ) = split( ';', $i ); chomp( $j1, $j2 ); if ( some condition ) { ### color node/box as green } else { ### color node/box as blue } $g-&gt;add_edge( $j2 =&gt; $j1 ); } $g-&gt;as_svg( "Graph.svg" ); </code></pre> <p><strong>Note:</strong> </p> <p>I am able to color edges based on condition but not nodes.</p> <p>Need your suggestion.</p>