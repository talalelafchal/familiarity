Is there a module that prints a list like `ls -C`?
<p>Is there a module that prints (or helps me printing) a list whose elements are strings (scalars) in a <code>ls -C</code> fashion?</p> <p>Something like the following imaginary program:</p> <pre><code>use strict; use warnings; use Unknown::Module; my $ls = new Unknown::Module (columns =&gt; 3); $ls -&gt; print_ls_C qw(abc def ghi jklmnop q rst uv wxy z); </code></pre> <p>The output should then be something like</p> <pre><code> abc jklmnop uv def q wxy ghi rst z </code></pre>
<pre><code>use Perl6::Form; my $cols = shift || 3; my @list = qw(abc def ghi jklmnop q rst uv wxy z); print form q/ {:[{*}[:} / x $cols, ( \@list ) x $cols ;;; __END__ abc jklmnop uv def q wxy ghi rst z </code></pre> <p>This is from example demo_columns , see more examples in <a href="https://metacpan.org/source/DCONWAY/Perl6-Form-0.04/demo" rel="nofollow">https://metacpan.org/source/DCONWAY/Perl6-Form-0.04/demo</a></p>