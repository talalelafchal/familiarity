How to delete the previous output in perl CGI script?
<p>My HTML code</p> <pre><code>&lt;html&gt; &lt;head&gt; &lt;title&gt;hello&lt;/title&gt; &lt;/head&gt; &lt;body bgcolor = pink&gt; &lt;form action ="hai.pl" method=post&gt; &lt;label&gt;enter your name&lt;/label&gt; &lt;input type = "text" name ="fname"&gt; &lt;input type = "submit" value = "go"&gt; &lt;/form&gt; &lt;/body&gt; &lt;/html&gt; </code></pre> <p>perl script </p> <pre><code>use CGI ':standard'; $firstname = param('fname'); print "&lt;html&gt;"; print "&lt;head&gt;"; print "&lt;title&gt;$firstname&lt;/title&gt;"; print "&lt;/head&gt;"; print "&lt;body&gt;"; print "&lt;h2&gt; hai $firstname &lt;/h2&gt;"; print "&lt;/body&gt;"; print "&lt;/html&gt;"; </code></pre> <p>When i give the first input into the html code that only print the many more times of the output but different kind of input. example first time i give the input MKHUSSAIN . which output is hai MKHUSSAIN . next time i give the input BOXER. which output is hai MKHUSSAIN. how can i clear this error?</p>
<p>Make that param line into a two part thing.</p> <pre><code>my $firstname=""; $firstname= $cgi-&gt;param("fname"); </code></pre> <p>Creating a empty string first also helps to empty it, everytime it is run. This is also a security conscious move. </p>
<p>Your code should only print out an error. There are a few things wrong with your perl script. First you need a shebang to indicate which interpreter will be running the file. Second you need to print a cgi prefix (the header function below) to indicate the type of content for the page.</p> <p>You should also include error and warnings support for your perl script. This will help you to easier identify problems. </p> <p>Look at:</p> <pre><code>#!/usr/bin/perl use strick; use warnings; use CGI ':standard'; my $firstname = param('fname') || ""; print header; print "&lt;html&gt;"; print "&lt;head&gt;"; print "&lt;title&gt;$firstname&lt;/title&gt;"; print "&lt;/head&gt;"; print "&lt;body&gt;"; print "&lt;h2&gt; hai $firstname &lt;/h2&gt;"; print "&lt;/body&gt;"; print "&lt;/html&gt;"; </code></pre> <p>Again, the example you posted wouldn't do anything but show errors. The changes I made actually does what you described as your objective.</p>
<pre><code>use CGI; $cgi = CGI-&gt;new(); $firstname = $cgi-&gt;param('fname'); print "&lt;html&gt;"; print "&lt;head&gt;"; print "&lt;title&gt;$firstname&lt;/title&gt;"; print "&lt;/head&gt;"; print "&lt;body&gt;"; print "&lt;h2&gt; hai $firstname &lt;/h2&gt;"; print "&lt;/body&gt;"; print "&lt;/html&gt;"; </code></pre> <p>use the 'CGI->new()' in every perl script then only execute the output which we expect.</p>