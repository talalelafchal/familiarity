perl hash ref operator is working weirdly
<p>I m having a single array ref consisting a list of hash refs.</p> <p>Here is the code:</p> <pre><code>use strict; use warnings; my $arrayRef = [ { 'URL' =&gt; 'http://example.com/1.jpg', 'ORD' =&gt; '1', }, { 'URL' =&gt; 'http://example.com/2.jpg', 'ORD' =&gt; '2', }, { 'URL' =&gt; 'http://example.com/3.jpg', 'ORD' =&gt; '3', }, ]; print $arrayRef-&gt;[0]-&gt;{URL},"\n"; # http://example.com/1.jpg print $arrayRef-&gt;[0]{URL},"\n"; # http://example.com/1.jpg </code></pre> <p>Since it is an hash ref i am using <code>-&gt;</code> operator to get a value, but i m getting the same value without using the <code>-&gt;</code> operator is it fine?</p>
<p>It's valid, a kind of syntactic sugar, and it refers to the same item. The <a href="http://perldoc.perl.org/perlreftut.html#Arrow-Rule">Perl references tutorial</a> writes about this</p>