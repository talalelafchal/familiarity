Issue while authorizing a web page using LWP perl
<p>I am trying to login into an URL using LWP perl</p> <p>My URL open ups with a pop up asking for username and password.When I try to manually log in ,it succeeds but through LWP,I am getting 401 error</p> <pre><code>my ($url) = @_; my $ua = LWP::UserAgent-&gt;new( keep_alive =&gt; 1, agent =&gt; "Mozilla/4.76 " ); push @{ $ua-&gt;requests_redirectable }, 'POST'; $ua-&gt;cookie_jar( HTTP::Cookies-&gt;new( file =&gt; "$ENV{HOME}/.cookies.txt" ) ); # timeout: $ua-&gt;timeout(30); print "\nMaking request to $url\n"; my $request = HTTP::Request-&gt;new('GET'); $request-&gt;url($url); my $response = $ua-&gt;request($request); my $user = "weblogic"; my $pwd = "Fusionapps1"; $ua-&gt;credentials( $url, "default", $user, $pwd ); #$request-&gt;authorization_basic( $user, $pwd ); my $response = $ua-&gt;request($request); if ( $response-&gt;is_success ) { $isLoginSuccessful = 1; } } </code></pre> <p>I have tried with the below thing as well</p> <pre><code>$request-&gt;authorization_basic( $user, $pwd ); </code></pre> <p>ERROR: Error 401--Unauthorized</p> <p>Kindly help!! Any help will be appreciated</p>
<p>To interact with a website and fill out forms such as logins, I suggest that you use <a href="https://metacpan.org/pod/WWW%3a%3aMechanize" rel="nofollow"><code>WWW::Mechanize</code></a>.</p> <p>Note, if the website contains JavaScript, you'll have to use something like <a href="https://metacpan.org/pod/WWW%3a%3aMechanize%3a%3aFirefox" rel="nofollow"><code>WWW::Mechanize::Firefox</code></a>.</p>